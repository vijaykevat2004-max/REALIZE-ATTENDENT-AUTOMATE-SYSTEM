services:
  - type: web
    name: hrms-ai-service
    env: python
    region: oregon
    buildCommand: pip install -r services/ai/requirements.txt
    startCommand: cd services/ai && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
